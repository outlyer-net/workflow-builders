---

name: Build

on:
  push:
  workflow_dispatch:

env:
  REGISTRY: git.outlyer.net

jobs:
  build:
    runs-on: docker
    steps:
      - uses: actions/checkout@v3

      - name: Build
        run: >
          env \
            REGISTRY=${{ env.REGISTRY }} \
            IMAGE=${{ env.GITHUB_REPOSITORY }} \
            ACTION=push \
            ./make.bash fedora
